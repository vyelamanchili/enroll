<%= form_for @person, {as: :person, url: employers_person_path(id: @person.id), method: :put, remote: true} do |f| %>
  <%= f.hidden_field 'person_id', @person.id %>
  <%= render "person_fields", f: f %>
<% end %>
