<%= form_for @quote, url: {action: "create"}, method: "post", class: "form-inline" do |f| %>
	<%= render "edit_form", f:f %>
	<%= submit_tag "Save Roster", class: 'btn btn-primary' %>
	<%= link_to "Back to Quote Cost/Comparison", broker_agencies_quotes_root_path, class: 'btn btn-primary'%>
<%end%>
