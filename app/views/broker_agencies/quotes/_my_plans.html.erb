<div class="container-fluid">
    <div>
      <h3>Step 4 - Select reference plans for comparison (up to 5)</h3>

      <h4>Showing <strong id="x-of-plans"><%= @plans.count %></strong> of <strong><%= @plans.count %></strong>  plans based on your selection criteria</h4>
      <div class="btn-group" data-toggle="buttons" style='height: 340px; overflow-y: scroll' id="quote-plan-list">
        <% @plans.each_with_index do |p,index| %>
        <label class="btn btn-small btn-primary <%= "active" if params['plans'].try(:include?, p.id.to_s) %>" >
          <input name="plans[]" <%= "checked" if params['plans'].try(:include?, p.id.to_s) %> type="checkbox" autocomplete="off" value="<%= p.id %>">
          <%= p.name %></label>
        <% end %>
      </div>
    </div>

      <br>
    	<%= submit_tag "Compare Costs", class: 'btn btn-primary', id: 'QuoteCalculation' %>
    	<%= submit_tag "Compare Plans", class: 'btn btn-primary', id: 'comparePlans' %>
    	<br><br>

</div>
